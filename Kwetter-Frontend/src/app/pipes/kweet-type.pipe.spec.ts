import { KweetTypePipe } from './kweet-type.pipe';

describe('KweetTypePipe', () => {
  it('create an instance', () => {
    const pipe = new KweetTypePipe();
    expect(pipe).toBeTruthy();
  });
});
