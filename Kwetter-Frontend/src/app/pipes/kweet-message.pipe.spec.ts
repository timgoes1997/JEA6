import { KweetMessagePipe } from './kweet-message.pipe';

describe('KweetMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new KweetMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
